---
- hosts: webservers
  tasks:
    - name: Log into private registry and force re-authorization
      docker_login:
        registry: ghcr.io
        username: bnymn
        password: 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
        reauthorize: yes

    - name: Install applications
      include_tasks: 05_create_applications/01_install.yml
      loop: "{{ applications }}"
      loop_control:
        loop_var: parent_item

    - name: Restart nginx
      service:
        name: nginx
        state: restarted
