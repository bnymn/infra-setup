---
- hosts: all
  tasks:
    - name: Update apt cache and install nginx
      apt:
        name: nginx
        state: latest
        update_cache: yes
    - name: start nginx
      service:
          name: nginx
          state: started
          enabled: yes